# A string in python can be sliced for getting a part of the strings.

name = "Alekhya"

print(len(name))  # result will be 7.

shortname = name[0:3] # which means start from index 0 all the way till 3 (excluding 3).

print(shortname) # result will be Ale

char = name[4]
print(char) # prints h